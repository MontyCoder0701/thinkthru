name: thinkthru
description: "App for Impulsive Thinking"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  intl: ^0.19.0
  sqflite: ^2.4.1
  json_annotation: ^4.9.0
  local_auth: ^2.3.0
  shared_preferences: ^2.3.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  json_serializable: ^6.9.2

flutter:
  uses-material-design: true
